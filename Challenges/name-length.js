

let myName = "Stephens"
let friend = "Stephen"

console.log(myName.length)
console.log(friend.length)




if (myName.length < friend.length){
    console.log(`${friend}'s name is longer than ${myName}`)
} 




else if (myName.length > friend.length){
    let difference = friend.length - myName.length
        console.log(`${friend}'s name is longer than ${myName} by ${difference} letters`)}
 else {
    console.log(`names are equal`) 
 }

        



